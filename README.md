# Rackspace Movie Watchlist Catalog

## Installation

- Run pip install -r requirements.txt
- execute `python app/movies.py`

## Running tests

- cd into app directory
- execute `nosetests -vs tests/test_app.py`
